list<string>listStr=new list<string>();
if(listStr.isEmpty()){
    system.debug('list is empty.come on.');
}
listStr.add('some string');
boolean listIsEmpty =listSt.isEmpty();
if(!listStr.isEmpty()){
    system.debug('list is empty');
}